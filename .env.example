# Optional OpenAI-compatible LLM backend configuration for 猫眼之后.
# Copy values into your shell environment before starting backend/main.py.
# The game and backend work without these variables by using local fallback text.

LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4.1-mini
